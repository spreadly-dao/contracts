{
  "name": "contract",
  "version": "1.0.0",
  "description": "",
  "main": "build_contracts.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "prettier --write '**/*.{js,ts}'",
    "build": "node build.js"

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mustache": "^4.2.0"
  },
  "devDependencies": {
    "prettier": "^3.1.1"
  }
}
